# Session 8 — v1.6 Parity Remediation

Session start: workspace refreshed via `git pull` (`5dbb2eb` → `30a9f4d`). The pull brought in only `docs/session_8.md`, which turned out to be an accidentally-committed raw streaming transcript of the v1.5 session (its content duplicates `session_7.md`; it is replaced by this log — no information lost). Re-read all four core docs (AGENTS, CLAUDE, README, PAD v1.5) plus `session_7.md`, `parity-remediation-v1.5.md` and the transcript, then validated the v1.5 claims by re-running the full gate on the existing build: lint 0 · typecheck 0 · **85/85** unit · build clean · **30/30** smoke — every claim held. Spot-checks: `.orb-btn-dark`/`.orb-btn-post`/`.orb-btn-cancel`/`.orb-pill-round`/`.orb-live-dot` primitives present, 1200px clamp + purple glow in `orbital-app.tsx`, `formatLongDate` in `calendar.ts`.

Fresh live-app crawl (`research/live-capture-s8/`, 20+ captures) — this round ran **two parallel agent-browser sessions** (live + the clone's production build) with a reusable side-by-side probe script (`scripts/par-probe.sh`) executing identical computed-style extraction on both. Four systemic findings:

- **The reference ships THREE card tiers; the clone shipped one.** Measured: standard pair `-5px 0.78` (buttons), deeper pair `-5px 0.92/0.36` (goal cards r16, task cards + activity feed r14), large pair `-8px 0.78/0.31` (dashboard panels, settings cards, goal-detail stats r16). The clone rendered nearly everything at the standard tier — every view read visibly flatter.
- **The reference sidebar is STICKY** (`position: sticky; top: 0; height ≈ viewport − 40px`) — the clock/TASKS STATUS stay pinned. The clone's sidebar stretched with the page: the clock sat at y≈1957, below a 900px fold.
- **The reference task card is flat and compact at radius 14, with actions OUTSIDE the card**: gray 11px/500 status chip, 14px/500 title, 12px one-line description, 11px meta — the 26px radius-7 action squares float at the row's top-right. The clone used 15px/600 titles, colored status text, 36px well buttons, and a latent cascade bug (`orb-card rounded-[14px]` rendered radius 16).
- **The reference mobile chrome is different from desktop**: a sticky full-bleed app bar (logo + user pill, 62px, bottom shadow) replaces the greeting header; the date card + ring sit side-by-side even at 390px; the bottom tab bar is a floating rounded-24 pill with an inset active highlight. The clone rendered the desktop header, stacked hero, and a flat full-width bar.

Other measured finds: dashboard grid gap 20px (clone 16); ring card `p-6` (circle 156px vs clone 168); stats panel `p 20/24` with `p 10/8` columns; activity card `p-0` with full-width rows (`p 14/18`, 30px solid icon circles `#C9B3F5`/`#2ECC8A`, 13px `square-check-big` glyphs) and the NPA well inset by 14px margins; goals card `p 20/18/0` with goal rows as inset wells + 60px rings; goals-view cards split into a 952px left column + 120px stats column with a **42px/500** percentage and flat gray 29×23 actions; the activity FEED groups under uppercase 10px/600 date labels with the latest entry as a hero card and "Online · N" (count only); Team buttons are radius-12 rectangles (not round pills) and the AI-Agents empty state sits in an inset well card; settings cards use the large tier (`p 22/24`, headers 13px, inputs 38px/13px, "Active Window" 12px/600 Title Case above the description); the AI badge is a solid `#EEEAE6` chip (r6, no icon). The check-in modal, add-task dialog, date picker and My Tasks tabs re-verified as still aligned. (The live session expired mid-crawl; re-authenticated.)

Remediation plan v1.6 (`docs/parity-remediation-v1.6.md`), validated file-by-file against the codebase before execution, then executed in full:

- **WS-1** card tiers: `.orb-panel` (large r16), `.orb-row-card` (deeper r14), `.orb-goal-card` (deeper r16) primitives; `.orb-task-blocked` re-scoped to the coral ring only; `.orb-pill-round` re-measured as a radius-12 rect.
- **WS-2** dashboard: 20px gaps, `.orb-panel` on all four cards, ring `p-6`, stats `p 20/24` + columns `p 10/8`, activity card `p-0` (header px 18/pt 14, NPA 14px margins `p 12/14`, rows `p 14/18` with 30px solid icons), goals card `p 20/18/0` with inset-well rows + 60px rings.
- **WS-3** task cards: flat radius-14 deeper rows with the whole card as the modal trigger; gray status chip (dot keeps the status color); 14/500 titles; 12px one-line descriptions; 11px meta with 11px lucide icons; solid AI chip; 26px r7 action squares pinned top-right outside the card; inline delete confirm preserved.
- **WS-4** goals view: two-column card (left `p 18/20`, right 120px `p 18/16`) with the 42px/500 percentage and flat gray 29×23 actions.
- **WS-5** activity feed, TDD: `groupActivityByDate` written RED first (8 specs — grouping order, labels, midnight boundaries, "Today" — **85 → 93** unit checks) then GREEN; view rebuilt with the hero card, date labels and per-day big cards; "Online · N".
- **WS-6** Team + Settings: radius-12 buttons + Title Case empty-state action; agents empty state in an inset well; large-tier settings cards, 13px headers, 38px/13px inputs (`Input`/`SelectTrigger` bases updated; dialog `h-11` overrides removed), "Active Window" above the description.
- **WS-7** shell: sticky sidebar (`lg:top-6 lg:h-[calc(100vh-3rem)]`); mobile app bar (logo + compact user pill); floating rounded-24 pill tab bar with inset active highlights; side-by-side mobile hero; the greeting header hidden below `lg`; per-view `max-w-[1100px]` wrappers removed (the shell owns the clamp).

Verification (WS-8): re-ran the side-by-side probes against the new build — grid gaps 20px, all four panels at the large tier (shadow strings byte-identical to the live), ring 156px inner circle, stats `p 20/24`, task cards 1072×111 r14 deeper with 26×26 r7 actions at the measured coordinates, goals card columns + 42px percentage, activity hero + date labels at 10px/1.2px, Team buttons 152×35 r12, settings panels `p 22/24` with 38px inputs, sticky sidebar (clock y 724 on a 900px viewport), mobile app bar 390×62 with the measured shadow, floating tab bar r24. Also fixed during verification: a dropped hero-section margin and the goal-card action radius (theme maps `rounded-lg` → 16px — explicit `rounded-[8px]` per the cascade rule). VLM cross-checks on dashboard, activity, team, mobile. Final gate: lint 0 · typecheck 0 · **93/93** unit · build clean · **30/30** smoke · placeholder sweep clean. All 12 README screenshots regenerated from the v1.6 build (login captured after a real logout).

Documentation aligned (WS-9): README (three-tier design system, sticky sidebar, mobile chrome, activity date groups, 93 checks, refreshed screenshots), AGENTS.md (card-tier facts, sticky sidebar, task-card spec, activity-groups seam, mobile chrome, 93 checks), CLAUDE.md (same in agent-contract form), PAD → v1.6 (revision block, §3.2 tree, §5.2 tier classes, §7 at 93 checks, §11 line counts re-measured), plus this log replacing the raw transcript.

Delivered as two commits on `main` (`:art: feat:` code + `:memo: docs:` docs), pushed through the SSH wrapper (`docs/ssh_git_wrapper_v3.py`) per the runbook in `docs/how-to-git-push-using-ssh-wrapper_SKILL.md`.
