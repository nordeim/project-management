# Session 7 — v1.5 Parity Remediation

Session start: workspace re-cloned fresh (the sandbox had been reset) at `main` `d23fee2` — confirming Sessions 1–5 landed: the v1.4 remediation was committed (`2b448b7`), its docs aligned (`a46feb6`), and the continuation log added (`d23fee2`, `docs/session_6.md`). Re-read all four core docs (AGENTS, CLAUDE, README, PAD v1.4) plus `session_5.md`, `session_6.md` and `parity-remediation-v1.4.md`, then validated the v1.4 claims by re-running the full gate on a fresh install: lint 0 · typecheck 0 · **80/80** unit · build clean (16 API routes + `/login`) · 30/30 smoke — every claim held. Spot-checks: neumorphic tokens (93 `orb-` references), `.orb-task-blocked` cascade fix, dialog base 500/radius-20, nullable-user store, rewrites exclude `/login`.

Fresh live-app crawl (`research/live-capture-s7/`, 21 captures) — this round ran **programmatic computed-style probes on both the live app and the clone's production build side-by-side**, plus VLM analyses, and surfaced three systemic findings the screenshot-only rounds had missed:

- **The reference's in-dialog primary buttons are dark, not neumorphic.** "Post Update" (`#2F2823`, radius 14, 32px, literal Title Case + Send icon, content-sized ~121px), "Continue" / "Add Task" (`#3A3A3A` bg, `#F1F1F0` text, radius 10) — while page-level actions (NEW GOAL, ADD TASK, DELETE) stay raised and dialog "Cancel" buttons are soft inset wells. The clone shipped neumorphic raised everywhere.
- **The reference main content is a clamped 1200px column on a glowing canvas**: `max-width: 1200px` wrapper (content ≈1072px at 1440 — the clone rendered unclamped), plus a fixed decorative purple radial gradient (`600px at 87.44% 95.38%, rgba(201,179,245,0.35) → transparent 70%`) that had gone unnoticed in every prior crawl.
- **The dashboard is a 2×2 grid**: [date card 330×180 + ring 180×180 | stats 526×180] over [activity 526 | goals 526] — not the clone's 3-across row. The ring is a **plain CSS inset circle** (no SVG), the date card's photo is a **bright day-hills image** (extracted from the reference as `public/day-hills.jpg`, 1024×576), and the greeting is 28px at every breakpoint.

Other measured finds: the check-in modal is **448px / radius 16** (form dialogs stay 500/20) with **plain radio labels** (no card wrappers), a **bordered transparent textarea** (1px `#D8D4CF`, radius 14) and the dark Post Update pill; goal-card chips carry a **light-purple pip (`#C9B3F5`) for every status** + inline red "· N blocked" + trailing `›`, with meta and date stacked on two lines; the date-picker popover is a **raised `#EEEAE6` card (260px, radius 16)** — not white — with today in bold purple and the trigger formatted **"September 20th, 2026"**; My Tasks ships **five filter tabs** (no "Need Help"); empty states render **directly on the canvas** (no card); Team's action buttons are round pills with Plus icons; Settings has an "Active Window" sub-header; the Log Out icon is 24px.

Remediation plan v1.5 (`docs/parity-remediation-v1.5.md`), executed in full:

- **WS-1** dark-primary hierarchy: `.orb-btn-dark` / `.orb-btn-post` / `.orb-btn-cancel` primitives in `globals.css`; applied across all six dialogs (Post Update, Continue, Generate Tasks, Add Task, Save Changes ×2, Send Invite/Create Agent) with well-styled Cancels.
- **WS-2** shell: `max-w-[1200px]` content clamp in `orbital-app.tsx` (per-view widths removed) + the fixed purple radial-glow overlay (pointer-events none, z-0; content at z-1).
- **WS-3** dashboard: 2×2 grid; date card 330×180 with `day-hills.jpg` @ 0.8 and the measured date square (radius 12, `clamp(28px,3.5vw,52px)` fw-300 numerals, 9px caps month line); 180×180 ring card with the CSS inset circle (`FaintRing` deleted as dead code); stats panel at 180px; greeting fixed at 28px.
- **WS-4** check-in modal: 448px/radius-16 override, plain 14px radio labels, `#D8D4CF`-bordered textarea (80px), dark content-sized Post Update pill.
- **WS-5** goals cards: light-purple pip chip with inline blocked count + chevron; two-line meta; pressed-in 6px track.
- **WS-6** date picker, TDD: `formatLongDate` written RED first (ordinals 1st/2nd/3rd, 11th–13th, 21st–23rd, all twelve months — **80 → 85** unit checks) then GREEN; popover restyled to the raised 260px card; today in bold purple; trigger shows the long format.
- **WS-7** view polish: cardless `EmptyState`, five My Tasks tabs, `.orb-pill-round` Team buttons with Plus icons, "Active Window" label, 24px Log Out icon, and the Agent Activity dot as a 7px `#2ECC8A` pulse (`.orb-live-dot` keyframe) instead of an expanding ping.

Verification (WS-8) surfaced two cascade bugs, both fixed with dedicated classes per the documented rule: utility overrides on custom-class buttons lose (`h-8`/`rounded-[14px]`/`normal-case` vs `.orb-btn-dark` → solved by the purpose-built `.orb-btn-post`), and `rounded-xl` maps to 20px in this theme's shadcn radius scale (the date square needed `rounded-[12px]`). A final VLM pass flagged the Post Update button as full-width — re-measured on live (121×32, content-sized) and corrected. Everything else verified programmatically against the live measurements: hero cells 528×180 (grid), ring 180×180 no-SVG with 168px circle, date square 50.4px fw-300, wizard Continue `#3A3A3A`/`#F1F1F0`/r10, check-in modal 448/16 with plain radios + bordered textarea + 131×32 dark pill, chip pip `#C9B3F5`, meta/date stacked (y295/y317), popover 260px `#EEEAE6` r16 with purple today and "September 20th, 2026" trigger, five tabs, cardless empty state, round Team pills, Active Window present — with zero console errors across the auth round-trip (logout → LOG IN → `/login?from_url=` → sign-in → shell).

Final gate: lint 0 · typecheck 0 · **85/85** unit · build clean · 30/30 smoke · placeholder/mock/TODO sweep clean. All 14 README screenshots regenerated from the v1.5 production build.

Documentation aligned (WS-9): README (2×2 dashboard, dark-primary + glow design-system notes, long-format date picker, 85 checks, `day-hills.jpg` in the hierarchy), AGENTS.md (dark-submit + clamp + glow + grid facts, the widened cascade warning covering `rounded-*`/`h-*`, five-tabs rule, cardless empty states, 85 checks), CLAUDE.md (same in agent-contract form), PAD → v1.5 (revision block, §7 at 85 checks, §11 line counts re-measured), plus this log.

Delivered as two commits on `main` (`:art: feat:` code + `:memo: docs:` docs), pushed through the SSH wrapper (`docs/ssh_git_wrapper_v3.py`) per the runbook in `docs/how-to-git-push-using-ssh-wrapper_SKILL.md`.
